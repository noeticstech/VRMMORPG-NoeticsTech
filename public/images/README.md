Drop the supplied artwork into these paths to replace the placeholder SVGs without changing component code:

- `hero-vr-world.svg`: hero image
- `about-vision.svg`: about section image
- `services-technology.svg`: services section image
- `projects-world-map.svg`: projects section image
- `team-creators.svg`: team section image
- `footer-digital-universe.svg`: footer image

The current SVG files are lightweight stand-ins so the project runs immediately. Replace them with your exported PNG or WebP assets and keep the same filenames, or update `src/config/site.ts`.
